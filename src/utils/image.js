/**
 * Mengoptimalkan URL gambar untuk tampilan web.
 * (Saat ini pass-through ke Firebase URL atau data lokal)
 */
export const getOptimizedImageUrl = (originalUrl, width = 600, quality = 80, height = null, resize = 'cover') => {
  return originalUrl;
};
